# Healthcare Backend (Django + DRF)

Backend system for managing healthcare data including authentication, patients, doctors, and mappings.

## Tech Stack
- Django
- Django REST Framework
- PostgreSQL
- JWT Authentication

## Setup

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
